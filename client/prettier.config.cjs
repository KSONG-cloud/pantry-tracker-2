/** @type {import("prettier").Config} */
module.exports = {
    semi: true, // add semicolons at the end
    singleQuote: true, // use single quotes
    trailingComma: 'es5', // adds trailing commas where valid in ES5
    printWidth: 80, // max line length
    tabWidth: 4, // indentation
    bracketSpacing: true, // spaces in object literals
    arrowParens: 'always', // always include parens in arrow functions
};
